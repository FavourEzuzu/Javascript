document.getElementById("#list")

// query selector is used to select any type of element individually
document.querySelector("#list a").style.color = "magenta"

// while the query select all is used to select all the element with the class or id specified


// we can separate the html, css and js into different files and folders
// this is called separation of concerns
// it makes the code more readable and easier to maintain
document.querySelector("h1").classList.add("huge");